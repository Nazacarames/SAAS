# 2026-03-05

- 01:08-01:10 (GMT-3) Cron hardening step: `integrationRoutes_idem.ts` `/messages` brought to parity with strict outbound idempotency controls.
  - Added max length guard (`IDEMPOTENCY_KEY_MAX_LENGTH = 120`) for header/body idempotency keys.
  - Added invalid character rejects (`[a-zA-Z0-9:_-.]` allowlist) instead of silent sanitization.
  - Added dual-header support + mismatch checks (`x-idempotency-key` vs `idempotency-key` and header vs body).
  - Added `waOutboundRequireIdempotencyKey` strict requirement (default true) in addition to retry-safe requirement.
  - Preserved min length enforcement and propagation to `SendOutboundTextService`.
  - Quick check: `Select-String` confirms new guards; file is currently untracked in git (`?? integrationRoutes_idem.ts`).
  - Next: either wire/import this hardened router as active integration endpoint or remove duplicate route file to avoid policy drift.

- 02:08-02:12 (GMT-3) Cron hardening step: `aiRoutes.ts` (`POST /meta/oauth/test-send`) aligned with strict outbound idempotency guardrails.
  - Added shared idempotency helpers in route module: `resolveOutboundRequireIdempotencyKey`, `IDEMPOTENCY_KEY_MAX_LENGTH=120`, strict normalizer and invalid-char detector.
  - Endpoint now accepts both `x-idempotency-key` and `idempotency-key`, rejects header/header and header/body mismatches.
  - Added explicit rejects for invalid chars and oversized keys (header/body) instead of permissive pass-through.
  - Enforced `waOutboundRequireIdempotencyKey` (secure default true) plus existing retry-safe key requirement and min-length check.
  - Quick check: `git diff -- aiRoutes.ts` confirms hardening delta; compile check not run here (workspace lacks TS toolchain).
  - Next: expose counters/alerts for these validation rejects in hardening-status endpoint (`metrics.outbound.idempotency_validation_*`).

- 13:16-13:19 (GMT-3) Cron hardening step: inbound webhook content-type fail-closed + metric/alert mínima.
  - `whatsappCloudRoutes.ts`: `POST /webhook` now rejects non-JSON `Content-Type` with `415` before envelope/signature parsing.
  - `whatsappCloudRoutes.ts`: added derived alert `inbound_invalid_content_type_blocked_spike` (warn >=2, critical >=6).
  - `ProcessCloudWebhookService.ts`: added `recordInboundInvalidContentTypeBlocked()` counter/signal (`inbound.invalid_content_type_blocked`, `inbound_invalid_content_type_blocked`).
  - Quick check: `git diff -- whatsappCloudRoutes.ts` and `Select-String` on new metric/signal passed.
  - Next: surface this counter in hardening summary recommendations (operator hint when >0).
