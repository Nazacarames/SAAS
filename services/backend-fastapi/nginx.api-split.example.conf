# Example: gradual /api cutover (Node + FastAPI)
# Node:    127.0.0.1:4000
# FastAPI: 127.0.0.1:4010

upstream node_api {
  server 127.0.0.1:4000;
}

upstream fastapi_api {
  server 127.0.0.1:4010;
}

# Optional canary map (header-controlled)
map $http_x_api_canary $use_fastapi_canary {
  default 0;
  "1" 1;
}

server {
  listen 443 ssl;
  server_name login.charlott.ai;

  # auth/me + users canary-ready
  location ~ ^/api/auth/me$ {
    if ($use_fastapi_canary = 1) { proxy_pass http://fastapi_api; break; }
    proxy_pass http://node_api;
    include /etc/nginx/snippets/proxy-common.conf;
  }

  location ~ ^/api/users/? {
    if ($use_fastapi_canary = 1) { proxy_pass http://fastapi_api; break; }
    proxy_pass http://node_api;
    include /etc/nginx/snippets/proxy-common.conf;
  }

  # read routes (stage 2)
  location ~ ^/api/(contacts|conversations)(/.*)?$ {
    proxy_pass http://fastapi_api;
    include /etc/nginx/snippets/proxy-common.conf;
  }

  location ~ ^/api/messages/\d+$ {
    proxy_pass http://fastapi_api;
    include /etc/nginx/snippets/proxy-common.conf;
  }

  # write routes (stage 3)
  location = /api/messages {
    proxy_pass http://fastapi_api;
    include /etc/nginx/snippets/proxy-common.conf;
  }

  # fallback everything else to Node
  location /api {
    proxy_pass http://node_api;
    include /etc/nginx/snippets/proxy-common.conf;
  }
}
